import { styled } from "styled-components";

export const Container = styled.div`
    display: flex;
    width: calc(100%-80px);
    padding: 40px;
    column-gap: 20px;
    row-gap: 40px;
    justify-content: space-between;
    flex-wrap: wrap;
`